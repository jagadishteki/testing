const AUTH_MODULE = '/auth/';
const CRM_MODULE = '/crm/';

export const URLs = {
    LOGIN_URL: `${AUTH_MODULE}login`,
    SIGNUP_URL: `${AUTH_MODULE}signup`,
    DASHBOARD_URL: `${CRM_MODULE}dashboard`,
}